# =============================================================================
# KoderKids wabot v3 — Railway n8n variables (values pulled from this repo)
# =============================================================================
# Sources: wabot/README.md, wabot/v1.txt (WAHA key), backend/.env (URLs),
#          wabot/v3.json FALLBACK_WAHA_BASE_URL.
#
# YOU MUST STILL SET:
#   - WEBHOOK_URL → your real Railway public URL for this n8n service
#
# WHATSAPP_BOT_KEY (Django) / BACKEND_WA_BOT_KEY (n8n) use the same 64-hex
# value as backend/.env and render.yaml (backend service).
# =============================================================================

# --- WAHA (X-Api-Key value taken from historical wabot/v1.txt workflow) ---
WAHA_BASE_URL=http://212.224.86.152:3000
WAHA_API_KEY=e703864150e6cc02ae8f2dee97803c7d344846bf796e17134e480d0013a30e47
WAHA_SESSION=default

# --- Django CRM (same API host as backend/.env NGROK_URL / README) ---
BACKEND_URL=https://koderkids-erp.onrender.com
# Same value as backend WHATSAPP_BOT_KEY and render.yaml WHATSAPP_BOT_KEY.
BACKEND_WA_BOT_KEY=363d09454dd9db87525b4ddbbf80aed04993f7b141518b10abb54675a4ce6f6b

# --- Sales WhatsApp (from wabot/README.md table; digits only) ---
SALES_REP_1=923339446254
SALES_REP_2=923329779989
SALES_REP_3=923185414080

# --- CRM deep-link in rep DMs (backend/.env FRONTEND_URL) ---
FRONTEND_URL=https://portal.koderkids.pk

# --- n8n on Railway: replace with your service’s public HTTPS root ---
WEBHOOK_URL=https://YOUR-N8N-SERVICE.up.railway.app/

# --- Recommended on Railway (generate once; keep stable across deploys) ---
# N8N_ENCRYPTION_KEY=

# --- Only if Code nodes still see empty process.env ---
# N8N_BLOCK_ENV_ACCESS_IN_NODE=false
